{
    "builds": [{
        "src": "videoconferencing/wsgi.py",
        "use": "@natanffs/vercel-python-wsgi/python",
        "config": { "maxLambdaSize": "15mb", "runtime": "python3.9" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "videoconferencing/wsgi.py"
        }
    ]
}
